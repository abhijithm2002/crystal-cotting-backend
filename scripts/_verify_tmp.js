// This scratch file was used while iterating during development and is
// superseded by scripts/verify-offline.js (the permanent, documented
// version of the same checks). Kept as a thin re-export only because the
// build sandbox's mounted filesystem does not permit deleting files.
module.exports = require('./verify-offline');
